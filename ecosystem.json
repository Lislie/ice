{
  "apps": [
    {
      "name": "Ice",
      "script": "server.js",
      "env": {
        "COMMON_VARIABLE": "ture"
      },
      "env_production": {
        "NODE_ENV": "production"
      }
    }
  ],
  "deploy": {
    "production": {
      "user": "root",
      "host": ["154.223.181.115"],
      "ref": "origin/master",
      "repo": "https://Lislie:bagacc821@github.com/Lislie/ice.git",
      "path": "/home/wwwroot/ice/production",
      "ssh_options": "StrictHostKeyChecking=no",
      "post-deploy": "npm install && pm2 startOrRestart ecosystem.json --env production",
      "pre-deploy-local": "echo 'Deploy Done'",
      "env": {
        "NODE_ENV": "production"
        // 这是一个测试
      }
    }
  }
}
